{
  "name": "chat-overlay",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "host:local": "vite preview --host 0.0.0.0",
    "proxy:twitch": "node proxy/server.mjs"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^8.0.10"
  }
}
