{
  "name": "chat-overlay",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "build": "npm run build:published",
    "build:local": "npm run typecheck && vite build --mode local-service",
    "build:published": "npm run typecheck && vite build --mode published",
    "serve:local": "npm run build:local && vite preview --outDir local",
    "serve:local:lan": "npm run build:local && vite preview --outDir local --host 0.0.0.0",
    "preview:published": "vite preview --outDir published",
    "proxy:twitch": "node proxy/server.mjs"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^8.1.5"
  }
}
